services:
  - type: web
    name: talkme
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: INVITE_CODE
        sync: false
      - key: JWT_SECRET
        sync: false
